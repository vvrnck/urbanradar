{
  "name": "crimeradar-ui",
  "version": "1.0.0",
  "description": "crimeradar frontend",
  "main": "index.js",
  "repository": "https://github.com/igarape/crimeradar-ui.git",
  "author": "vvrnck",
  "license": "MIT",
  "engines": {
    "node": ">=14.10.0 <=16.16.0"
  },
  "scripts": {
    "serve": "nodemon index.js",
    "build": "cd app/ && yarn install && yarn build",
    "dev": "cd app/ && yarn install && yarn dev",
    "start": "yarn build && yarn serve",
    "deploy:dev": "cd app/ && yarn deploy:dev",
    "deploy:prod": "cd app/ && yarn deploy:prod",
    "account": "node config.js"
  },
  "dependencies": {
    "connect-history-api-fallback": "^2.0.0",
    "express": "^4.18.2",
    "nodemon": "^2.0.21"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
}
